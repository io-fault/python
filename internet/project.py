identity = 'https://fault.io/project/python/fault.internet'
name = 'internet'
abstract = 'Internet protocol implementations (HTTP, DNS, and SMTP) for Python'
icon = '📡'

fork = 'darpa'
status = 'flux'

controller = 'fault.io'
contact = 'mailto:critical@fault.io'

version_info = (0, 0, 0)
version = '.'.join(map(str, version_info))
